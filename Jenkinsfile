pipeline {
    agent any

    stages {
        stage('initial stage') {
            steps {
                sh '''
                    echo "Bank Microservices CI Pipeline "
                '''
            }
        }
    }
}